services:
  pixelfin:
    build: .
    container_name: pixelfin
    ports:
      - "1280:1280"
    environment:
      - TZ=America/Chicago
    volumes:
      - ./output:/app/output
      - ./assets:/app/assets
      - ./data:/app/data
    restart: unless-stopped
